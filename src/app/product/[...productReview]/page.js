export default function productReview({params}){
  console.log(params); //{details :1/2/3/....} extract query params in server
  
  return <div>
    <h1>this is the page for product review and catch all routes</h1>
  </div>
}